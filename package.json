{
  "name": "gametracker-root",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "heroku-postbuild": "cd backend && npm install",
    "start": "cd backend && node src/index.js"
  }
}
